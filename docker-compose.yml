# no version needed since 2020

services:
  jekyll:
    image: bretfisher/jekyll-serve
    volumes:
      - .:/site
    ports:
      - "4000:4000"
  crawler:
    build:
      context: .
      dockerfile: Dockerfile.crawler
    volumes:
      - .:/site
    env_file:
      - .env